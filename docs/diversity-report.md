# 大规模多样性验证报告(真实 GLM 生产路径)

> 日期:2026-08-21 · 模型:glm-4-flash · 引擎版本:dev/v2-overhaul@431d673
> 方法:scripts/diversity-scan.mts 起真实服务(LLM=real),13 局 × 24 步全流程,
> 13 部门全覆盖,难度 good/random 混合,共 312 事件 / 1207 选项 / 574 次 LLM 调用,
> 墙钟 64 分钟。原始数据:data/final-scan-13games.json。

## 结论(对照用户算法诉求)

| 诉求 | 指标 | 首轮扫描(修复前) | 本次终扫 | 状态 |
| --- | --- | --- | --- | --- |
| 2. 重复文案/选项卡 | 一局内标题重复(bigram≥0.55) | 12/13 局,37 次 | **0** | ✅ |
| 2. 套话标题 | 泛化标题数(暗流涌动类) | 14 次/9 部门 | **0** | ✅ |
| 2. 重复选项卡 | 一局内选项重复(≥0.8) | 逐字重复多次放行 | **17**(1.4%) | ⚠️ 已修复,见下 |
| 1. 故事连续性 | 剧情衔接字段覆盖率 | — | **100%** | ✅ |
| 1. 事实矛盾 | 职级错误修正后残留 | 存在 | **0**(且本轮源头 0 错误) | ✅ |
| 3. 属性变化 | 选项非零效果率 | — | **100% (1207/1207)** | ✅ |
| 4. 官职事实 | 职级违规(修正后) | 原始案例复现 | **0** | ✅ |
| 5. 升官快乐 | 每局晋升次数 | ~10 抉择无变化 | good 均值 **3.6** / random **2.8** | ✅ |

解析成功率 100%,13/13 局完整通关,NPC 名册复用率 94.2%(跨事件人物延续),
LLM 代理 0 次 5xx,延迟 p50 22s / p95 83s(含去重重试的多次上游调用)。

## 遗留 17 次选项重复的定性与处置

逐条核对(原始数据可复现):约 1/3 为逐字重复(含句号差异),系当时
enforceFreshness 在"干净选项<2"时保留碰撞最轻 2 条的旧兜底放行;
其余为包含式近似(短选项字符集 ⊂ 长选项,如「向李明汇报情况,寻求建议。」
⊂「…寻求他的建议和帮助」),阈值边缘(0.80-0.9)。

两类漏点已在 f13d8f8(reviewer 第三轮)根治:
1. 兜底改为引擎合成一对互异且与池零碰撞的选项,绝不逐字放行;
2. 事件内部槽位互抄(同一文案写进 A/B 槽)也先剔除再过滤。

已另起 8 局确认扫描(引擎=f13d8f8)验证 withinGameChoiceDup → 0,
结果追加于下节。

## 确认扫描(f13d8f8 引擎,8 局)

> 数据:data/confirm-scan-8games.json

标题重复 0 / 泛化 0 / 属性非零 100% / 职级残留 0 维持;选项重复 17 → 3,
另 1 局因上游 60s 超时的错误语义不可重试而中断(ABORTED)。3 例残留与
中断均已定位根因并修复(82e4bad):

1. **去重池 60 条截断**:24 步 × 4 选项 = 96 条,第 15 步起开局文案被挤出
   池,与早期文案 1.00 全等的照抄查不出来——3 例残留全部撞的是早期文案。
   修复:池上限提至 200(提示词只读最近 12 条,扩大零成本)。
2. **超时语义**:AbortError 原消息「This operation was aborted」不含
   timeout 字样,重试正则失配,瞬时超时被当成致命错误。修复:服务端统一
   改写为「upstream … timeout after Xms」,重试正则补 abort。

## 终验扫描(82e4bad 引擎,8 局 × 24 步)

> 数据:data/final-confirm-scan.json · 8/8 局完整通关,0 中断,0 上游错误

| 指标 | 结果 |
| --- | --- |
| 一局内标题重复 | **0** |
| 一局内选项重复 | **0**(17 → 3 → 0) |
| 泛化套话标题 | **0** |
| 属性非零效果 | **100% (737/737)** |
| 职级错误残留 | **0** |
| 解析成功率 | **100%** |
| 剧情衔接覆盖 | **100%** |
| NPC 名册复用率 | 94.8% |
| 延迟 p50/p95 | 28s / 82s |
| 结局 | GREAT 7 / GOOD 1(good+random 策略;bad 策略 BAD 结局由单测覆盖) |

三轮真实 GLM 扫描(13 局初扫 → 8 局确认 → 8 局终验,共 ~1300 次
上游调用、29 局、696 事件)收敛到全指标达标。

## 多样性供给面

- 标题:312 事件 309 个互异标题(跨局重复率 1.0%,均为不同局独立生成);
- 选项:1207 条文案 1200 个互异(去标点口径);
- 结局分布:GREAT 12 / GOOD 1 —— 本扫描策略只含 good/random;bad 策略
  的 BAD 结局与晋升拦截由单测覆盖(tests/unit/gameEngine.test.ts 坏玩家全流程)。
- 15/312 事件仅 2 个选项(4.8%):解析端与去重剔除的合法下限,非缺陷。

## 复现

```bash
set -a && . ./.env && set +a
GAMES=13 CONCURRENCY=3 SCAN_PORT=3393 npx tsx scripts/diversity-scan.mts
```

指标口径:标题重复用词组级 bigram Jaccard(阈值 0.55),选项重复用
bigram 与字符包含取大者(阈值 0.8)——见 src/engine/dedup.ts 与
tests/unit/dedup.test.ts。
